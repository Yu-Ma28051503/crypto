/*
 * base32を実装したプログラム
 *
*/

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>

#define MAX 100

int main(void)
{
    char str[MAX];
    char *p;

    printf("Input string: ");
    fgets(str, MAX, stdin);

    p = str;

    

    return 0;
}